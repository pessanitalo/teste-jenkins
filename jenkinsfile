pipeline{
    agent any

    stages{
       stage('Build Image'){
            steps{
                scrips{
                     sh "docker version"  // Verifica se o Jenkins pode acessar o Docker
                     sh "docker images"  // Lista as imagens disponíveis
                }
            }
       } 
    }
}